// If i = index of layer to be incremented, will require
// layer[i-1].numNeurons edges pointing to new node, and 
// layer[i+1].numNeurons edges pointing to nodes in next layer
export async function add_neuron(svg, layerIndex, weights) {



}